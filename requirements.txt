# Core
PyQt6
requests
yt-dlp
qtawesome
python-dotenv  # Load API keys from .env file
dotenv
# Video/Audio Processing
pillow
ffmpeg-python

# AI Features
openai-whisper
edge-tts
deep-translator
google-generativeai  # Gemini Pro translation
openai  # OpenAI GPT-5/GPT-5 mini translation
opencv-python  # AI subtitle removal (inpainting)
easyocr  # Accurate subtitle text detection
# Optional: Fast transcription on Apple Silicon (M1/M2/M3)
mlx-whisper; platform_system == "Darwin" and platform_machine == "arm64"

# TikTok/Douyin Download
pyktok
playwright

# Build
pyinstaller

# Optional - for audio duration detection
mutagen
